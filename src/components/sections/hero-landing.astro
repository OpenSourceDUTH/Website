---
import { cn } from "@/lib/utils";
import { buttonVariants } from "@/components/ui/button";
import { Icon } from "astro-icon/components";
import { siteConfig } from "@/config/site";
const { joinUsLabel = "Join Us"} = Astro.props;
---

<section class="space-y-6 py-8 sm:py-16 lg:py-20">
  <div class="container flex max-w-5xl flex-col items-center gap-4 sm:gap-5 text-center px-4 sm:px-6 lg:px-8">
    <!-- Should add link to the blog article -->
    <a
      href="http://opensource.cs.duth.gr/blog/grcpc-hits-duth-and-we-are-behind-it/" 
      class={cn(
      buttonVariants({ variant: "outline", size: "sm", rounded: "full" }),
      "px-4"
      )}
      target=""
    >
      <span class="mr-3">🎉</span> We are co-hosting GRCPC 2025!{" "}
    </a>
   

    <h1
      class="text-right text-balance font-heading text-6xl leading-tight tracking-tight
             sm:text-[min(12vw,10rem)] 
             md:text-[min(14vw,12rem)] 
             lg:text-[min(15vw,142px)]"
    >
      OpenSource{" "} <br>
      <span class="text-gradient_indigo-purple font-extrabold">
        DUTH
      </span>
    </h1>

    <div class="flex justify-center space-x-2 md:space-x-4">
      <a
        href="/apply" target="_blank"
        class={cn(buttonVariants({ size: "lg", rounded: "full" }), "gap-2")}
      >
        <span >{joinUsLabel}</span>
        <Icon name="lucide:arrow-right" class="size-4" />
      </a>
      <!--
      <a
        href={siteConfig.links.github}
        target="_blank"
        rel="noreferrer"
        class={cn(
          buttonVariants({
            variant: "outline",
            size: "lg",
            rounded: "full",
          }),
          "px-5"
        )}
      >
        <Icon name="github" class="mr-2 size-4" />
        <p>
          <span class="hidden sm:inline-block">Star on</span> GitHub{" "}
          <!-- <span class="font-semibold">{nFormatter(stars)}</span> -->
        <!--</p>
      </a> -->
    </div>
  </div>
</section>
