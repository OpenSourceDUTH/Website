---
import { buttonVariants } from "@/components/ui/button";
import { cn } from "@/lib/utils";
import { Icon } from "astro-icon/components";
import MainLayout from "@/layouts/main-layout.astro";
---

<MainLayout title="Η σελίδα δεν βρέθηκε" description="Η σελίδα που ψάχνετε δεν υπάρχει.">
  <section class="container flex items-center justify-center min-h-[60vh] py-16">
    <div class="flex flex-col items-center text-center space-y-8 max-w-md">
      <!-- 404 Number -->
      <div class="relative">
        <h1 class="text-8xl md:text-9xl font-bold text-muted-foreground/20 select-none">
          404
        </h1>
        <div class="absolute inset-0 flex items-center justify-center">
          <Icon name="lucide:search-x" class="size-16 md:size-20 text-muted-foreground" />
        </div>
      </div>

      <!-- Content -->
      <div class="space-y-4">
        <h2 class="text-2xl md:text-3xl font-heading font-bold">
          Η σελίδα δεν βρέθηκε
        </h2>
        <p class="text-muted-foreground text-lg">
          Ωχ! Η σελίδα που ψάχνετε δεν υπάρχει. Μπορεί να έχει μετακινηθεί, διαγραφεί ή να εισάγατε λάθος URL.
        </p>
      </div>

      <!-- Actions -->
      <div class="flex flex-col sm:flex-row items-center gap-4">
        <a
          href="/el/"
          class={cn(buttonVariants({ size: "lg" }), "gap-2")}
        >
          <Icon name="lucide:home" class="size-4" />
          Αρχική
        </a>
        <button
          onclick="history.back()"
          class={cn(buttonVariants({ variant: "outline", size: "lg" }), "gap-2")}
        >
          <Icon name="lucide:arrow-left" class="size-4" />
          Πίσω
        </button>
      </div>

      <!-- Help -->
      <div class="text-sm text-muted-foreground">
        <p>Ακόμα δεν μπορείτε να βρείτε αυτό που ψάχνετε;</p>
        <a 
          href="/el/blog" 
          class="underline hover:text-foreground transition-colors"
        >
          Δείτε το ιστολόγιό μας
        </a>
        ή
        <a 
          href="https://github.com/OpenSourceDUTH" 
          target="_blank"
          rel="noopener noreferrer"
          class="underline hover:text-foreground transition-colors"
        >
          επισκεφθείτε το GitHub μας
        </a>
      </div>
    </div>
  </section>
</MainLayout>

<script>
  // Add some interactive effects
  document.addEventListener('DOMContentLoaded', () => {
    const icon = document.querySelector('[data-icon="lucide:search-x"]') as HTMLElement;
    if (icon) {
      icon.addEventListener('mouseenter', () => {
        icon.style.transform = 'scale(1.1) rotate(5deg)';
        icon.style.transition = 'transform 0.3s ease';
      });
      
      icon.addEventListener('mouseleave', () => {
        icon.style.transform = 'scale(1) rotate(0deg)';
      });
    }
  });
</script>
